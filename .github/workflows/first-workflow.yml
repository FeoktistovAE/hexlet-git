name: hello-world
on: push
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
    - name: make-helloworld
      run:
          pip install poetry
          make install
          make say-hello
