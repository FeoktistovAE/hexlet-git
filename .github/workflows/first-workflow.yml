name: hello-world
on: push
jobs:
        build:
                runs-on: ubuntu-latest
                steps:
                        - name: make-helloworld
                          run: make say-hello!
